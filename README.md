# Hedera Wallet EIP-155 & HIP-820 Example

## Setup

```sh
# install dependencies
yarn install

# setup VITE_REOWN_PROJECT_ID environment variable
cp .env.local.example .env.local

# run app
yarn start
```
